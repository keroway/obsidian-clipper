name: typecheck

# main への push と PR で TypeScript の型チェックを走らせる。
# Renovate PR のチェック土台にもなる。
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: latest

      # frozen-lockfile で bun.lock の整合性も同時に検証する。
      # bunfig.toml の minimumReleaseAge は frozen-lockfile では効かないが、
      # ローカル/CI で同じ lockfile を使うことで「ローカルで通った age gate を
      # CI でもそのまま使う」前提が成立する。
      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun run typecheck
